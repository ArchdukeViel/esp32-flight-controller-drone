Prompt 2 — I2C bus scan
========================

Status: Build gate PASSED.

What exists:
- i2c_bus component (v6 handle-based, i2c_master_bus_handle_t)
- Bus init + scan in app_main
- board_config constants for I2C pins/freq

What's next (Prompt 3):
- MPU6050 detection: WHO_AM_I, raw read, scaled read
- But only after this phase is confirmed on hardware (flash + monitor logs)

Remaining warnings:
- MCPWM, ESC, WiFi, PID, receiver, safety state machine all DISABLED.
- Do not connect ESCs or propellers.